<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shop.repository.ItemRepository">
    <resultMap id="itemResultMap" type="com.shop.entity.Item">
        <id property="id" column="ITEM_ID"/>

        <result property="itemNm" column="ITEM_NM"/>
        <result property="price" column="PRICE"/>
        <result property="itemDetail" column="ITEM_DETAIL"/>
        <result property="stockNumber" column="STOCK_NUMBER"/>
        <result property="itemSellStatus" column="ITEM_SELL_STATUS"/>
        <result property="regTime" column="REG_TIME"/>
        <result property="updateTime" column="UPDATE_TIME"/>
        <result property="modifiedBy" column="MODIFIED_BY"/>
        <result property="createdBy" column="CREATED_BY"/>

    </resultMap>

    <insert id="save" parameterType="com.shop.entity.Item">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT ITEM_SEQ.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO ITEM (
        ITEM_ID, ITEM_NM, PRICE, ITEM_DETAIL, STOCK_NUMBER, ITEM_SELL_STATUS, REG_TIME, UPDATE_TIME,CREATED_BY
        ) VALUES (
        #{id}, #{itemNm}, #{price}, #{itemDetail}, #{stockNumber}, #{itemSellStatus}, SYSDATE, SYSDATE,#{createdBy}
        )
    </insert>

    <select id="findById" resultMap="itemResultMap">
        SELECT * FROM ITEM WHERE ITEM_ID = #{id}
    </select>

    <update id="update" parameterType="com.shop.entity.Item">
        UPDATE ITEM SET
        ITEM_NM = #{itemNm},
        PRICE = #{price},
        ITEM_DETAIL = #{itemDetail},
        STOCK_NUMBER = #{stockNumber},
        ITEM_SELL_STATUS = #{itemSellStatus},
        MODIFIED_BY =  #{modifiedBy},
        UPDATE_TIME = SYSDATE
        WHERE ITEM_ID = #{id}
    </update>

    <update id="updateStock" parameterType="com.shop.entity.Item">
        UPDATE ITEM
        SET
        STOCK_NUMBER = #{stockNumber},
        UPDATE_TIME = SYSDATE
        WHERE ITEM_ID = #{id}
    </update>

    <select id="findByItemNm" resultMap="itemResultMap">
        SELECT * FROM ITEM WHERE ITEM_NM = #{itemNm}
    </select>

    <select id="findByItemNmOrItemDetail" resultType="com.shop.entity.Item">
        SELECT * FROM ITEM WHERE ITEM_NM = #{itemNm} OR ITEM_DETAIL = #{itemDetail}
    </select>

    <select id="findByPriceLessThan" resultMap="itemResultMap">
        <![CDATA[
    SELECT * FROM ITEM WHERE PRICE < #{price}
    ]]>
    </select>

    <select id="findByPriceLessThanOrderByPriceDesc" resultMap="itemResultMap">
        <![CDATA[
    SELECT * FROM ITEM WHERE PRICE < #{price} ORDER BY PRICE DESC
    ]]>
    </select>

    <select id="findByItemDetail" resultMap="itemResultMap">
        SELECT * FROM ITEM
        WHERE ITEM_DETAIL LIKE '%' || #{itemDetail} || '%'
        ORDER BY PRICE DESC
    </select>

    <select id="findByItemDetailByNative" resultMap="itemResultMap">
        SELECT * FROM ITEM i
        WHERE i.ITEM_DETAIL LIKE '%' || #{itemDetail} || '%'
        ORDER BY i.PRICE DESC
    </select>

</mapper>